# 227 Rollback

Because 227 adds files only, rollback is straightforward: revert the merge commit. No persisted Flight Recorder schema currently in production is changed by this increment, and no migration is required.
