Implementation target: in app.py, inside the browser voice-preference discovery script that builds URLSearchParams and currently does `if (changed) window.parent.location.replace(`${window.parent.location.pathname}?${params}`);`, replace only that full-navigation call with a non-navigating History API update such as `window.parent.history.replaceState(null, '', `${window.parent.location.pathname}?${params}`);`. Preserve the existing params construction and voice/David availability semantics. Do not change trading behavior or unrelated voice transport logic.
