# GS265: dependency manifest touch forces Streamlit Cloud to rebuild the Python runtime
# after authoritative market-clock changes, preventing stale imported modules from
# surviving a hot app rerun across deployments.
streamlit==1.59.2
pandas==2.3.3
numpy==2.5.1
requests==2.34.2
paho-mqtt==1.6.1
webull-openapi-python-sdk==2.0.16
