# GS445 deployment marker: force one clean Streamlit Community Cloud runtime so
# append-only Candidate History / Flight Recorder exports reuse incremental gzip state.
# No dependency versions change in this deployment-only activation.
# GS535 deployment marker: align bounded continuation re-ignition VWAP ceiling
# with the canonical +2.0% Entry Ready guard; all other continuation safeguards remain.
# GS534 deployment marker: show true Expansion Assessment survivors separately
# from canonical Entry Ready count in the Architecture Funnel. Presentation only.
# GS533 deployment marker: allow a bounded established-trend continuation
# re-ignition to satisfy only the SuperTrend trigger lock when fresh participation,
# expansion, near-VWAP geometry, and 3m re-acceleration all agree.
# GS532 deployment marker: observe a separate 3m-retest/lower-timeframe-repair
# Entry Ready counterfactual without changing canonical entry authority.
# Existing VWAP anti-chase, participation, expansion, readiness, and execution remain unchanged.
# GS531 deployment marker: Flight Recorder preserves populated Scanner V2
# participation/structure gate diagnostics; architecture audit is fallback-only.
# No live qualification, ranking, readiness, execution, or order behavior changes.
# GS530 deployment marker: bridge canonical Scanner V2 enrichment into the live
# Walter Architecture before Expansion so trigger/qualified_for_entry/GS529 truth exists.
# Membership and architecture stage thresholds remain unchanged.
# GS529 deployment marker: record observational Entry Ready shadow calibration
# comparing the discrete ST trigger lock with fresh ordered 1m/3m maturation.
# No qualification, readiness, alert, execution, or order authority changes.
# GS528 deployment marker: ENTRY READY is reserved for qualified_for_entry=True;
# non-qualified records expose SETTING UP plus exact blockers.
# No qualification, threshold, readiness, execution, or order changes.
# GS527 deployment marker: surface fresh 30s bullish flips with exceptional
# volume/dollar-flow acceleration as attention-only EARLY SURGE WATCH events.
# No entry, qualification, readiness, anti-chase, retest, execution, or order changes.
# GS526 deployment marker: stop materially 3m-ST-stretched runners from presenting
# as fresh DEVELOPING unless a new higher-timeframe maturation event re-arms attention.
# Presentation semantics only; no trading-authority changes.
# GS525 deployment marker: expire stale 30s-derived Developing urgency after five
# minutes unless fresh higher-timeframe maturation re-arms attention.
# No discovery, ranking, readiness, anti-chase, retest, execution, or order change.
# GS524 deployment marker: audible health retest now proves the parent-window path
# with a practical two-strike bell and parent-window speech preference.
# No dependency versions change in this deployment-only activation.
# GS523 deployment marker: sample deeper Webull RVOL as forensic shadow evidence only.
# Shadow rows never enter Walter's production discovery or trading authority.
# No dependency versions change in this deployment-only activation.
# GS522 deployment marker: full Session Backup keeps disk-backed ZIP creation but
# uses the same click-deferred bytes transport that succeeded live for GS510.
# No dependency versions change in this deployment-only activation.
# GS504 deployment marker: force one clean Streamlit Community Cloud runtime so
# the final categorical LOOK NOW bell and entry-urgency siren load everywhere.
# No dependency versions change in this deployment-only activation.
# GS506 deployment marker: force one clean Streamlit Community Cloud runtime so
# background session-backup workers replace the prior synchronous backup fragment.
# No dependency versions change in this deployment-only activation.
# GS507 deployment marker: force one clean runtime so streamed backup members use
# ZIP64 when the active Candidate History exceeds the classic ZIP member limit.
# No dependency versions change in this deployment-only activation.
# GS508 deployment marker: force one clean runtime so prepared backup links use the
# origin-root /app/static path instead of inheriting Streamlit's /~+/ frontend route.
# No dependency versions change in this deployment-only activation.
# GS509 deployment marker: force one clean runtime so prepared archives download via
# Streamlit's native deferred file-like transport instead of runtime static serving.
# No dependency versions change in this deployment-only activation.
# GS510 deployment marker: force one clean runtime so compact forensic analysis
# bundles replace multi-hundred-megabyte full backups for routine ChatGPT review.
# No dependency versions change in this deployment-only activation.
# GS442 deployment marker: force one clean Streamlit Community Cloud runtime so
# the operator alignment card renders Walter's canonical 30s/1m/3m evidence ladder.
# No dependency versions change in this deployment-only activation.
# GS441 deployment marker: force one clean Streamlit Community Cloud runtime so
# the final action-only LOOK NOW / entry-urgency audio signatures load everywhere.
# No dependency versions change in this deployment-only activation.
# GS440 deployment marker: force one clean Streamlit Community Cloud runtime so
# Live Opportunity Feed signal-to-noise and same-scan operator priority load cleanly.
# No dependency versions change in this deployment-only activation.
# GS439 deployment marker: force one clean Streamlit Community Cloud runtime so
# fresh DO NOT CHASE extreme banners yield immediately to workable operator states.
# No dependency versions change in this deployment-only activation.
# GS436 deployment marker: force one clean Streamlit Community Cloud runtime so
# the final enriched Opportunity State ordering boundary is hard-bound everywhere.
# No dependency versions change in this deployment-only activation.
# GS435 deployment marker: force one clean Streamlit Community Cloud runtime so
# due-deadline AutoScan owner handoff is loaded across every live session.
# No dependency versions change in this deployment-only activation.
# GS434 deployment marker: force one clean Streamlit Community Cloud runtime so
# the fragment-owned Flight Recorder download boundary is loaded everywhere.
# No dependency versions change in this deployment-only activation.
# GS429 deployment marker: force one clean Streamlit Community Cloud runtime so
# the NumPy-backed, bit-identical SuperTrend recurrence is loaded everywhere.
# No dependency versions change in this deployment-only activation.
# GS428 deployment marker: force one clean Streamlit Community Cloud runtime so
# the merged GS424-GS428 late-runtime cache/timing/cadence chain is actually loaded.
# No dependency versions change in this deployment-only activation.
# GS387 deployment marker: force one clean Streamlit Community Cloud runtime so
# the permanent checkout-vs-loaded-runtime freshness detector is installed.
# No dependency versions change in this deployment-only activation.
# GS385 deployment marker: force Streamlit Community Cloud to rebuild the
# Python runtime so the merged GS384 diagnostic presentation code is loaded.
# No dependency versions change in this deployment-only nudge.
# GS362: pin the current Streamlit runtime with fragment/runtime fixes validated
# for Walter's periodic fragment, st.status, and reconnect execution model.
streamlit==1.62.0
pandas==2.3.3
numpy==2.5.1
requests==2.34.2
paho-mqtt==1.6.1
webull-openapi-python-sdk==2.0.16